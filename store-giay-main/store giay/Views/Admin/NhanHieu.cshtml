@model IEnumerable<store_giay.Models.Nhanhieu>

@{
    ViewBag.Title = "NhanHieu";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>NhanHieu</h2>

<p>
    @Html.ActionLink("Create New", "CreateNH")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenNhanhieu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenNhanhieu)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditNH", new { id=item.idNhanhieu }) |
            @Html.ActionLink("Details", "DetailsNH", new { id=item.idNhanhieu }) |
            @Html.ActionLink("Delete", "DeleteNH", new { id=item.idNhanhieu })
        </td>
    </tr>
}

</table>
